import requests
import time 
from datetime import datetime 


def fetch_data():

    url ="https://api.coingecko.com/api/v3/simple/price'"

    params={
        "ids":"Bitcoint,Ethireum",
        "vs_currencies":"usd
    }

    response=requests.get(url,params=params)

    data=response.json()

    bitcoin_data=data['Bitcoint']['usd']
    ethrm_data=data['Ethireum']['usd']

    timeDateFormat=datetime.now().strftime("%Y-%m-%d %H:%M:$S")